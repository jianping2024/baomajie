# 首页原型

本目录提供一个不依赖框架的可运行首页原型，用来先验证信息架构、视觉方向和移动端布局。

启动：

```bash
cd prototype
npm start
```

然后打开：

```text
http://localhost:3000
```

如果要用手机访问，把地址换成你这台电脑的局域网 IP，例如：

```text
http://192.168.1.23:3000
```

说明：

- 当前原型先使用矢量占位图和渐变卡片，不依赖外部图片资源。
- 这是首页原型，不是最终产品工程。
